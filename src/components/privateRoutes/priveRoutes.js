// import React from "react";
// import { Navigate } from "react-router-dom";

// const priveRoutes = () => {
//   const token = localStorage.getItem("token") || null;
//   token ? <Navigate to="/" /> : <Navigate to="/reg" />;
// };

// export default priveRoutes;
